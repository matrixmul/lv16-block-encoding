flowchart TD
  target["Target circuit: MatrixMul LV16"]
  algorithm["Algorithm"]
  optimization["Optimization"]

  target --> algorithm
  target --> optimization

  algorithm --> Editable["Editable entrypoint: src/matmul/mod.rs"]
  Editable --> Width["Declare a real 41-qubit QASM register"]
  Width --> SameWidth["Emit mathematical same-width MatrixMul oracle family"]
  SameWidth --> Prep["h on all 41 declared qubits"]
  SameWidth --> ZPhase["same_width/z deterministic rz phases"]
  SameWidth --> MatrixEdge["same_width/matrix_edge nearest-neighbor cx-rz-cx parity phases"]
  SameWidth --> XMixer["same_width/x_mixer h-rz-h blocks on logical system qubits"]
  Prep --> QASM["Submitted OpenQASM uses h, rz, and cx only"]
  ZPhase --> QASM
  MatrixEdge --> QASM
  XMixer --> QASM
  QASM --> Validation["Validation compares 41q candidate against math:same-width-matrixmul#qubits=41"]

  optimization --> Baseline["Latest accepted 42q baseline score 283,024.067175"]
  optimization --> WidthScore["41q same-width score reduces qubits and oracle-specific gate family"]
  WidthScore --> GateShape["Nearest-neighbor matrix edges avoid projected 42q ladder/workspace terms"]
  GateShape --> SubmitGate["Promote only after 9024 trusted shots, package, and validate"]
